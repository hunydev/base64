{
  "name": "basexx-converter",
  "version": "1.0.0",
  "private": true,
  "description": "SEO-optimized BaseXX converter (binary, text, and bytes) for GitHub Pages",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf docs/*"
  }
}
